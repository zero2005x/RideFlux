<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- BLE permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <!--
        The @HiltAndroidApp application class is wired up below so that
        Hilt can bootstrap the SingletonComponent. The MainActivity
        NavHost, launcher icon, and Theme.RideFlux will be added in the
        next phase (Compose UI scaffolding); until then the manifest
        deliberately stays free of an intent-filtered launcher so the
        app compiles / installs but does not yet expose an entry point.
    -->
    <application
        android:name=".RideFluxApplication"
        android:allowBackup="false"
        android:supportsRtl="true" />

</manifest>
