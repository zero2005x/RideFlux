# RideFlux HUD — R8/ProGuard rules
# Minification is disabled for this module (see build.gradle.kts), so
# this file is intentionally empty. Add keep rules here if you enable
# `isMinifyEnabled = true` for release builds.
