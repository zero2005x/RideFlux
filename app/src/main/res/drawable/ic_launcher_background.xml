<!--
  RideFlux launcher icon — background.
  Dark radial wash from midnight navy to near-black so the cyan
  foreground reads crisply on any system launcher background.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <path android:pathData="M0,0 L108,0 L108,108 L0,108 Z">
        <aapt:attr xmlns:aapt="http://schemas.android.com/aapt"
            name="android:fillColor">
            <!-- gradientRadius = 77 covers the full canvas (center-to-
                 corner distance is ~76.4) so no hard-black corner
                 strip appears if the drawable is ever rendered as a
                 full square. -->
            <gradient
                android:type="radial"
                android:centerX="54"
                android:centerY="54"
                android:gradientRadius="77"
                android:startColor="#0F1A2E"
                android:endColor="#000000" />
        </aapt:attr>
    </path>

</vector>
