# Team A Attestation

I (we), the undersigned specification authors, attest that:

1. The specification bundle in this directory
   ([PROTOCOL_SPEC.md](PROTOCOL_SPEC.md), [TERMS.md](TERMS.md),
   [TEST_VECTORS.md](TEST_VECTORS.md), [CHANGELOG.md](CHANGELOG.md))
   contains **no source code** from the external reference project and no
   pseudocode that mirrors its control flow.

2. The specification uses **independently chosen** terminology. It does not
   reuse package names, class names, method names, variable names, or other
   distinctive identifiers from the reference project.

3. The specification is intended to be the **sole input** delivered to
   Team B. No other information about the reference project's internal
   structure will be transmitted to Team B by the signers.

4. Where behavior is uncertain, the specification marks the ambiguity
   explicitly rather than guessing.

5. The signers have not authored, and will not author, implementation code
   for the modules covered by this specification in this repository while
   they hold Team A status.

---

## Signatures

| Name | Role | Date (YYYY-MM-DD) | Signature |
| --- | --- | --- | --- |
| Liang-Ting Lin (zero2005x) | Specification author (Team A) | _pending_ | _pending_ |
| Liang-Ting Lin (zero2005x) | Clean-room coordinator (witness) | _pending_ | _pending_ |

> **Process note.** Team A and the Coordinator are currently the same
> individual. This is permitted but reduces the independence of the witness
> role. Whenever feasible, the Coordinator role should be reassigned to a
> separate person before the next spec snapshot is approved for handoff.
>
> **Team B identity.** For the current rewrite, Team B is an AI assistant
> running in a dedicated chat session that has never been exposed to the
> reference repository or to the legacy quarantined files listed in
> [../../PROVENANCE.md](../../PROVENANCE.md) §1. Team B's outputs are
> reviewed by the Coordinator against
> [../audit/AUDIT_CHECKLIST.md](../audit/AUDIT_CHECKLIST.md) before being
> merged.
