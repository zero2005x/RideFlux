# Specification Changelog

All notable changes to the protocol specification bundle are recorded here,
newest first. Each entry is dated (ISO 8601). Breaking changes bump the
minor version; editorial changes bump the patch suffix.

## Unreleased

- Initial scaffolding of `PROTOCOL_SPEC.md`, `TERMS.md`, `TEST_VECTORS.md`,
  and `ATTESTATION.md`. No protocol content yet.
