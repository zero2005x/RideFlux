<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE permissions. Same surface as :app so the HUD can own its
         own GATT session when run standalone on the glasses.
         Legacy BLUETOOTH_ADMIN is supplied by :data:ble for scanning. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Fine location is only a runtime scan gate on API <= 30. Lint
         requires COARSE to be declared whenever FINE is requested. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="30" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:name="com.rideflux.hud.HudApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- screenOrientation="nosensor": Hardware dumpsys tests on the
             reference AR glasses (RV101 / RG_glasses) confirm the physical
             display panel is natively 480x640 portrait (rotation 0).
             Hardcoding "portrait" breaks landscape AR glasses (such as 1080p
             optics) by rotating the display 90 degrees sideways. Using "nosensor"
             locks rendering to each device's natural panel orientation while
             ignoring accelerometer tilt, preventing sudden jarring screen
             rotations when the rider tilts their head mid-ride. -->
        <activity
            android:name="com.rideflux.hud.HudActivity"
            android:exported="true"
            android:screenOrientation="nosensor"
            android:theme="@android:style/Theme.Material.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
